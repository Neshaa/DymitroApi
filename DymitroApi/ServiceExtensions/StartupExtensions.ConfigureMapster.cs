using System.Reflection;
//using Intelisale.KnjazMilos.Application.Configuration;
//using Mapster;
//using MapsterMapper;

namespace Intelisale.DymitroApi.ServiceExtensions
{
    public static class ConfigureMapsterExtensions
    {
        public static void ConfigureMapster(this IServiceCollection services)
        {
            //var typeAdapterConfig = TypeAdapterConfig.GlobalSettings;
            //typeAdapterConfig.Scan(Assembly.GetAssembly(typeof(MapsterConfig)));
            //var mapperConfig = new Mapper(typeAdapterConfig);

            //services.AddSingleton(typeAdapterConfig);

            //services.AddScoped<IMapper, ServiceMapper>();
        }
    }
}
